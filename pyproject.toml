[build-system]
build-backend = "poetry.masonry.api"
requires = ["poetry>=0.12"]

[tool]
[tool.poetry]
description = ""
name = "flasktemplate"
authors = ["Andrew Wason"]
version = "0.1.0"
[tool.poetry.dependencies]
flask = "^1.1"
python = "^3.8"
[tool.poetry.dev-dependencies]
